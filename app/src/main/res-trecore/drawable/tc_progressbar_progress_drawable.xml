<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 背景 -->
    <item
        android:id="@android:id/background"
        android:drawable="@drawable/tc_shape_rect_solid_gray_light_corners" />

    <!-- 进度条 -->
    <item android:id="@android:id/progress">
        <clip android:drawable="@drawable/tc_shape_rect_solid_theme_corners" />
    </item>

    <!-- 二级进度条 -->
    <item android:id="@android:id/secondaryProgress">
        <clip android:drawable="@drawable/tc_shape_rect_solid_theme_alpha_corners" />
    </item>
</layer-list>